import React from 'react'
import LoginClient from './LoginClient'

const page = () => {
    return (
        <React.Fragment>
            <LoginClient/>
        </React.Fragment>
    )
}

export default page